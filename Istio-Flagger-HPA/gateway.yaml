apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: bjp-gateway
  namespace: bjp
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "bjp.game.webdevprojects.cloud"